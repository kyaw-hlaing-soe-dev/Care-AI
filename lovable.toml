[run]
install = "bun install --cwd frontend"
dev = "bun run --cwd frontend dev"
typecheck = "bun run --cwd frontend typecheck"
build = "bun run --cwd frontend typecheck && bun run --cwd frontend build"
lint = "bun run --cwd frontend lint"
format = "bun run --cwd frontend format"
