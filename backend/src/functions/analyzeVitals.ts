/**
 * Analyze Vitals function handler.
 *
 * Future flow:
 * auth -> validate vitals -> calculate score -> save reading -> call OpenRouter
 * -> normalize analysis -> save analysis -> return safe response.
 */
export {};
